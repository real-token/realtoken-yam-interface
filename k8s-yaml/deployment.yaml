apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: yam-realtoken
  name: yam-realtoken
spec:
  selector:
    matchLabels:
      app: yam-realtoken
  template:
    metadata:
      labels:
        app: yam-realtoken
    spec:
      containers:
      - image: <IMAGE>
        imagePullPolicy: IfNotPresent
        name: yam-realtoken
        ports:
        - containerPort: 3000
          protocol: TCP